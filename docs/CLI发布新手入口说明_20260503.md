# CLI Beginner Guide

The public CLI path is intentionally small.

## Smoke Test

```bash
python scripts/public_smoke_test.py --rounds 1 --seed 42
```

## Demo Run

```bash
python simulation_runner.py --rounds 2 --seed 42
```

The output is JSON and includes the seed, round summaries, available properties, and completed transactions.
