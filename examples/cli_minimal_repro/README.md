# CLI Minimal Reproduction

Run from the repository root:

```bash
python scripts/public_smoke_test.py --rounds 1 --seed 42
```

Windows helper:

```powershell
powershell -ExecutionPolicy Bypass -File .\examples\cli_minimal_repro\run_mock_smoke.ps1
```
